<% if Current.admin %>
Logged in as: <%= Current.admin.name %><br>
<= link_to 'Edit Password', edit_password_path %>
<%= button_to 'Logout', logout_path, method: :delete %>
<% else %>
<%= link_to 'Sign Up', sign_up_path %>
or
<%= link_to 'Login', sign_in_path %>
<% end %>
<h1>Foo corp<h1>
<h1> Attendance</h1>
<p> <%= notice %> </p>
<%= form_with model: @employee, url:sign_up_path do |f| %>
  <p>
  <%= f.label 'Private number' %><br>
  <%= f.text_field :private_number %>
  </p>
  <p>
  <%= f.submit 'Check' %>
  </p>
<% end %>